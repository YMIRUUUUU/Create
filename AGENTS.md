# Repository Guidelines

- Run `./gradlew help` after making changes to ensure Gradle is set up correctly.
- Prefer a single commit per feature or fix.
- Verify `git status` shows a clean working tree before finishing.
- When running tests or builds fails due to environment limitations, note it in your PR description.
